waypointapp:
  build: .
  ports:
  # host:container
    - "5858:5757"
    - "5888:8888"
  environment:
    ENV: DEV
  volumes:
  # realdir:containerdir
  - ./app:/app
  # add "public folder here for shared drive"
  #- /mnt/srv/public:/public
  - ./data:/data

